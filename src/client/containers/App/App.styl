@import './base.styl'
@import './components.styl'

:local(.container)
	display flex
	flex-direction column
	min-height 100%

:local(.content)
	overflow-x hidden
	flex 1
	display flex
	flex-direction column
	